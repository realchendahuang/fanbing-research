# 研究方法与证据纪律

## 1. 研究问题，而不是还原“完整的人”

本项目研究范冰公开作品中的实践、观点和变化。公开材料是有限样本；没有公开的经历不是可以由模型补全的空白。结论只对材料范围负责，不声称掌握本人所有想法、动机、收入、关系或能力。

默认单位是作品、一次明确的公开事件、一条可定位主张和一组有条件的模式。把结论写成“材料 A 与 B 支持这个解释”，而不是“他内心一定这样想”。

## 2. 四类内容必须分开

| 类型 | 数据位置 | 可以怎样写 | 不可以怎样写 |
| --- | --- | --- | --- |
| 可查证的页面或作品事实 | `claims` 的 `documented` | 页面署名、公告日期、目录存在 | 页面存在就证明产品有效 |
| 本人陈述、回顾或受访回答 | `claims` 的 `self_report` | 作者自述使用该流程 | 已独立证实其绩效或因果 |
| 研究者的解释 | `patterns` 与案例分析 | 多份材料可能呈现某种重复做法 | 总结了他的全部行为规律 |
| 供读者执行的方案 | `lessons`、`experiments` | 建议做一个可失败的小实验 | 这是范冰亲自推荐、已经实测成功 |

`documented` 也可能只是“作者页面确实这样写”：例如作者仓库给出的出版月份，仍需出版社记录才能进一步核实。查阅每条 `limits`，不要只看类型。

## 3. 来源质量不能用单一等级替代

不把“本人博客”一律排成最高可信度。它适合证明本人公开表达过什么，未必适合证明收入、增长归因或实际效果。访谈刊在第三方网站，也可能仍然只是本人自述。

同时记录：作者/发布渠道、原始或转述、时间、实际阅读范围、是否独立、定位信息、权利说明。多个页面转述同一人，不构成多份独立验证。`independence_group` 用于提示这一点；`case_family_count` 只数观察情境，不是独立信源数或可信概率。

## 4. 阅读范围是硬约束

- `page_read`：已核对 `verification_scope` 写明的正文范围。书籍仓库通常只是 README、目录和许可，不等于逐章读书。
- `partial_read`：只阅读明确范围；不得对未读部分下结论。
- `index_only`：只核对目录或入口，不能据此摘要文章正文。
- `shownotes_only`：只读节目说明，不视为听过录音。要区分主持人、嘉宾、编辑与转述者。

本轮没有保存网页全文快照。`checked_at` 是研究者核对时间，不是原始网页的发布时间、修改时间、可用性保证或抓取完成率。`archive_url: null` 表示未验证快照；不得拼接一个看起来像存档的链接。

## 5. 日期精度不得伪造

资料至少可能有：写作/录音时间、首发时间、转载时间、页面修改时间、事情发生时间。本轮数据单独记录来源 `published_at` 和事件 `date_role`；混淆时保留说明。

只知道年份就写 `YYYY`，月份写 `YYYY-MM`，不知道就留 `null`。不使用 1 月 1 日作为占位。年度回顾的标题年份不能直接当发布年；书籍出版与作者后来公开电子全文必须是不同事件。来自归档目录的日期，记录相应 `date_source_ids`。

## 6. 模式如何建立、推翻或降级

每个模式必须有支持主张、适用情境、反向材料或反例检索记录、其他可能解释、当前信心、什么新证据会削弱它。

`low / medium / high` 表示维护者对“这个有限模式得到材料支持”的暂时判断，**不是对人物的评级、人格测量或成功率**。不得仅按链接数量自动升级。当前五项没有 `high`：资料集中于作者自述，缺少独立结果验证。

同一工具衍生出的两个版本属于有关联的案例谱系。周刊里引用他人的文章，不算范冰独立提出的观点。把书中案例当成作者亲自操作的项目同样是错误。

“找到反向观点”也不自动等于矛盾：先比较讨论对象、时间、受众、约束与目标。无法确认时用“张力”“待核实”，不使用人格指控。

## 7. 预测、结果与负面样本

预测只有在原始说法包含可以核对的对象、时间与结果标准时才登记。没有截止时间或目标可操作化的口号留在观点笔记，不事后改造成预测。解释相反结果之前先核查原文上下文。

失败、停止、成本、未验证结果必须留在记录中。未找到失败不等于不存在；资料断层不等于没有活动。当前 `predictions.json` 与 `experiments.json` 为空是有意的：没有为了完整而制造记录。

## 8. 使用边界

只处理公开且有权访问的材料。不绕过登录、付费、访问控制，不收集私人地址、电话、家庭关系等无关信息，不重新披露明确撤回的私人内容。引用私人对话截图中的第三方细节前应特别谨慎；本库不收录这些细节。

不通过风格模仿冒充本人，不替本人代言，也不把研究解释成读心。研究政治人物时只记录中立事实、具名观点和政策差异，不做政治选择推荐、人物打分、选举胜负推测或针对个人的政治说服。

## 9. 发布前检查

核对来源与引用定位；核对日期性质；区分自述与结果；检索反例；检查私人信息与版权；人工审阅 AI 产物；最后运行离线检查。程序可以发现错误引用、失效的本地路径或结构不一致，**不能证明历史真实、判断网页版权或替代人工阅读**。
