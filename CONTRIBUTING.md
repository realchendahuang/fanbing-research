# 如何贡献

欢迎补旧博客入口、可靠日期、原创案例分析、反例、音频定位与事实纠错。优先处理[队列](research/QUEUE.md)，不要以收集数量代替研究质量。

## 最小一次贡献

先查看 `data/sources.json` 是否已有同一作品。相同文章的转载不能算独立来源；新网址与新版本可以登记，但应说明关联。添加来源记录时提供实际读到的范围和原文定位，不能只粘一个标题。

需要新增结论时，在 `data/claims.json` 中用 `documented` 或 `self_report` 标记，填写 `limits`。再关联事件、作品或模式。纯线索只进任务队列，不要急着升级成结论。

编辑原创 Markdown 时，在事实段旁链接来源卡，例如 `[S018](../../sources/cards/S018.md)`。推断明确写成推断，并写出替代解释。

## 数据规则

UTF-8、JSON 两空格缩进、稳定 ID；日期遵循真实精度。未知写 `null` 或明确说明，不填猜测。所有字段的用途见 [数据说明](docs/DATA.md)，模板见 [templates](templates/README.md)。

模式要解释适用条件、反向材料与如何被削弱。独立来源比重复转载更有用。作品条目要区分作者、受访者、主持人、编者、嘉宾等角色。

## Pull Request 必须回答

说明本次问题、实际读取的来源、精确阅读范围、主要更改、反例检索和限制。标注是否使用 AI、是否人工复核。不要提交原文大段复制、个人隐私或无证据指控。

提交前运行 `build`、`validate`、`build --check` 和单元测试。新增文件的路径要可点击。测试只验证仓库结构，不取代事实检查。

## 纠错优先

指出哪一条主张不准确、为什么、原始证据在哪里即可。维护者应保留合理分歧，不用“粉丝/黑粉”给贡献者分类。权利请求和隐私问题按 [NOTICE](NOTICE.md) 处理。
