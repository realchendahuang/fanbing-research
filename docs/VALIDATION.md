# 本次打包验证记录

验证日期：2026-09-25。版本：0.2.0。环境：Python 3.13.5。

## 已执行

- `python scripts/research.py validate`：通过规范数据类型、真实日期、ID、跨表引用与本地文件链接检查。
- `python scripts/research.py build --check`：生成视图与规范数据一致。
- `python -m unittest discover -s tests -v`：24 项测试通过，包括错误日期、重复来源、缺失引用、错误路径、生成文件漂移和未填结果的实验。
- 以本机可用的独立 JSON Schema 库交叉检查了全部 10 份 schema 及对应数据；运行本仓库不需要该第三方库。
- 以本机 YAML 解析器检查 GitHub 配置、CITATION.cff 与 Skill frontmatter；检查通过不等于已在所有宿主中安装验证。
- 检查主张表中的 70 处链接锚点引用，未发现缺失。
- 检查交付文本不包含本机绝对目录和聊天工具专用引用标记。

## 没有执行

没有在你的 GitHub 账户运行 Actions；没有独立人工完成研究复核；没有验证所有外链的当前可用性或存档版本；没有逐章读两本书、核听播客、执行历史工具、审计经营数字或购买课程。

工具测试说明仓库能够生成、检索和检查自己的数据，不证明研究结论已经获得独立事实验证。后续修改后应重新运行所有检查，本报告只描述本次打包时的情况。
